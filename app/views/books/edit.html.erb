<h1>Editing Book</h1>

<h2>New book<h2>

<%= form_with model: @book, url:book_path(@book.id), method: :patch do |f| %>

<h4>title</h4>
<%= f.text_field :title %>

<h4>Body</h4>
<%= f.text_area :body %>

<%= f.submit 'update book' %>
<% end %>